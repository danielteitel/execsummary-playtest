# Executive Summary, playtest build

Generated output. Nothing here is written by hand and nothing here is the source.
Rebuilt and force-pushed by `tools/phone.py --publish` from the game project, so
this repository holds one commit and no history worth reading.

Play it at https://danielteitel.github.io/execsummary-playtest/

Add it to the home screen on a phone. It caches itself on the first load and plays
with the network off after that.
